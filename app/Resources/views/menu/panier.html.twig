{# == panier == #}

{% block panier %}
  <div class="navbar-right"><a href="{{ path('dm_shopmode_panier') }}">Mon panier: </a>{{ articleQuantity }}
    {% if articleQuantity < 2 %} article
    {% else %} articles
    {% endif %}
  </div>
{% endblock %}
